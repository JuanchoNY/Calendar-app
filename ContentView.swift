import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("Calendar")
                .font(.largeTitle)
                .navigationTitle("My Calendar")
        }
    
